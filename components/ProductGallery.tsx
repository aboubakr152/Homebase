import { Product, placeholderImage } from '@/lib/products';
export function ProductGallery({ product }: { product: Product }) { const images=[product.mainImage, ...product.gallery].filter(Boolean); const first=images[0]?.url || placeholderImage; return <div><div className="gallery-main"><img src={first} alt={product.mainImage?.alt || product.name}/></div><div className="gallery-thumbs">{(images.length?images:[{url:placeholderImage,alt:product.name}]).map((image,i)=><img key={i} src={image?.url || placeholderImage} alt={image?.alt || `${product.name} image ${i+1}`} loading="lazy" />)}</div></div> }
